interface:
  display_name: "pitch.dog Writing"
  short_description: "Do the real job. Sound like us doing it."
  default_prompt: "Use $pitchdog-writing to write this with our candid, warm, playful attention while preserving its real job, medium, facts, and speaker."
policy:
  products:
    - chatgpt
    - codex
    - atlas
  allow_implicit_invocation: true
